name: "sustainable_toystore"

services:
  # .NET Backend
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      DOCKER_BUILDKIT: 1

  # React frontend
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    environment:
      DOCKER_BUILDKIT: 1